export const metadata = {
  title: "Account settings · Flourish Roots Hair",
  description: "Manage your profile and contact details.",
};

export default function SettingsLayout({ children }) {
  return children;
}
