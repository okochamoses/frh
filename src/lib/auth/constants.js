export const AUTH_MODAL = {
  SIGN_IN: "SIGN_IN",
  SIGN_UP: "SIGN_UP",
};

export const VALIDATION = {
  EMAIL_REGEX: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
  PHONE_REGEX: /^\+?\d{10,15}$/,
  MIN_PASSWORD_LENGTH: 8,
};

export const STORAGE_KEYS = {
  TOKEN: "token",
  USER: "user",
};